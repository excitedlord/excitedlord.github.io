diff --git a/assets/css/styles.css b/assets/css/styles.css
index 5f6b40b..2bc4936 100644
--- a/assets/css/styles.css
+++ b/assets/css/styles.css
@@ -41,6 +41,65 @@ html[data-theme="light"] {
   --glass-shadow: rgba(60, 70, 120, 0.14);
 }
 
+/* ---------- Preloader ---------- */
+.preloader {
+  position: fixed;
+  inset: 0;
+  z-index: 9999;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  background: var(--bg);
+  transition: opacity 0.6s var(--ease), visibility 0.6s var(--ease);
+}
+
+.preloader-mark {
+  font-family: "Space Grotesk", "Inter", sans-serif;
+  font-weight: 700;
+  font-size: clamp(2.5rem, 8vw, 5rem);
+  letter-spacing: 0.08em;
+  background: var(--accent-grad);
+  -webkit-background-clip: text;
+  background-clip: text;
+  color: transparent;
+}
+
+.preloader-mark::after {
+  content: "";
+  display: inline-block;
+  width: 0.06em;
+  margin-left: 0.05em;
+  border-right: 2px solid var(--accent-2);
+  animation: preloader-blink 0.8s step-end infinite;
+}
+
+@keyframes preloader-blink {
+  50% { border-color: transparent; }
+}
+
+.preloader.hide {
+  opacity: 0;
+  visibility: hidden;
+  pointer-events: none;
+}
+
+body.preloading {
+  overflow: hidden;
+}
+
+body.preloading .aurora,
+body.preloading main,
+body.preloading .nav,
+body.preloading .footer {
+  visibility: hidden;
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .preloader-mark::after {
+    animation: none;
+  }
+}
+
 /* ---------- Reset ---------- */
 *,
 *::before,
diff --git a/assets/js/main.js b/assets/js/main.js
index a11059e..8370652 100644
--- a/assets/js/main.js
+++ b/assets/js/main.js
@@ -7,6 +7,34 @@
   const doc = document;
   const root = doc.documentElement;
 
+  /* ---------- Preloader ---------- */
+  const preloader = doc.getElementById("preloader");
+  const preloaderMark = doc.getElementById("preloaderMark");
+  const body = doc.body;
+
+  function revealSite() {
+    preloader?.classList.add("hide");
+    body.classList.remove("preloading");
+    preloader?.addEventListener("transitionend", () => preloader.remove(), { once: true });
+  }
+
+  if (preloader && preloaderMark) {
+    const word = "SKM";
+    let i = 0;
+    const typePreloader = () => {
+      preloaderMark.textContent = word.slice(0, i);
+      i++;
+      if (i <= word.length) {
+        setTimeout(typePreloader, 160);
+      } else {
+        setTimeout(revealSite, 450);
+      }
+    };
+    typePreloader();
+  } else {
+    body.classList.remove("preloading");
+  }
+
   /* ---------- Theme ---------- */
   const themeToggle = doc.getElementById("themeToggle");
   const savedTheme = localStorage.getItem("theme");
diff --git a/index.html b/index.html
index 1f6df22..402b55c 100644
--- a/index.html
+++ b/index.html
@@ -17,7 +17,7 @@
   <title>Sujith K. Mandala — Data Scientist & AI Engineer</title>
 
   <link rel="icon"
-    href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🧠</text></svg>" />
+    href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.85em' font-size='90'>♚</text></svg>" />
 
   <!-- Fonts -->
   <link rel="preconnect" href="https://fonts.googleapis.com" />
@@ -27,9 +27,24 @@
     rel="stylesheet" />
 
   <link rel="stylesheet" href="assets/css/styles.css" />
+
+  <noscript>
+    <style>
+      .preloader { display: none !important; }
+      body.preloading .aurora,
+      body.preloading main,
+      body.preloading .nav,
+      body.preloading .footer { visibility: visible !important; }
+    </style>
+  </noscript>
 </head>
 
-<body>
+<body class="preloading">
+  <!-- Preloader -->
+  <div class="preloader" id="preloader" aria-hidden="true">
+    <span class="preloader-mark" id="preloaderMark"></span>
+  </div>
+
   <!-- Animated aurora background -->
   <div class="aurora" aria-hidden="true">
     <span class="blob blob-1"></span>
